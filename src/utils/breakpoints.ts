const size = {
    xs: '1000px'
    /*sm: ‘768px’,
    lg: ‘1200px’,*/
   }
   const device = {
    xs: `(max-width: ${size.xs})`
    /*sm: `(min-width: ${size.sm})`,
    lg: `(min-width: ${size.lg})`*/
   }
   export default {size, device}